import * as assert from 'assert';
import { Indexer } from '../indexer';

test('Wow, Amazing Coding Experience. Good DevEx', () => {
    assert.strictEqual(1, 1);
    assert.strictEqual(Indexer, Indexer);
});
